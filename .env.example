# Rename to .env for local dev or set via hosting dashboard (Netlify/IONOS).
# NOTE: VITE_* variables are exposed to the browser (not secrets).

VITE_FORMSPREE_ID=your_formspree_id_here
VITE_TURNSTILE_SITEKEY=your_turnstile_sitekey_here
